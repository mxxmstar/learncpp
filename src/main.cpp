#include <iostream>
#include "log/logmanager.h"
int main() {
    // LogManager& log_manager = LogManager::getInstance();
    // log_manager.Init();
	std::cout << "Hello, World!" << std::endl;
    return 0;
}