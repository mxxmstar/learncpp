# 🎯 Video Processing Platform V1.0 - 模块整合与发布计划

## 📋 项目概述

**目标**：将所有分散的模块代码整合到 `modules/web/service/` 中，构建一个统一的、生产就绪的视频处理服务平台。

**当前状态**：
- ✅ 基础架构已搭建完成
- ✅ 各功能模块独立开发完成
- ✅ gRPC 通信机制正常工作
- ⚠️ 模块分散在多个目录，需要统一整合

**V1.0 愿景**：
- 单一的服务入口点
- 清晰的模块层次结构
- 完整的 API 文档
- 自动化测试覆盖
- 生产环境部署支持

---

## 🏗️ 当前架构分析

### 现有模块清单

```
modules/
├── log/              # ✅ 日志模块（基础层）
├── net/              # ✅ 网络模块（HTTP/WebSocket/TCP）
├── config/           # ✅ 配置管理模块
├── sqlite/           # ✅ 数据库模块
├── puller/           # ✅ 视频拉流模块（ZLMediaKit）
├── camera/           # ✅ 相机管理模块
├── decoder/          # ✅ FFmpeg 解码模块
├── preprocess/       # ✅ 视频预处理模块（OpenCV）
├── postprocess/      # ✅ 视频后处理模块（OSD等）
├── grpc/             # ✅ gRPC 基础设施模块
├── alg/              # ✅ 算法处理模块（gRPC 客户端）
├── videopipeline/    # ✅ 视频流水线模块
├── zlmediakit/       # ✅ ZLMediaKit 集成模块
└── web/              # ⭐ 目标整合模块
    ├── api/          # HTTP API 路由
    └── service/      # 服务管理容器
```

### 依赖关系图

```
基础层 (Infrastructure):
  log_lib → spdlog, fmt
  config_lib → yaml-cpp, log_lib
  sqlite_lib → SQLite3, log_lib

网络层 (Network):
  net_lib → Boost.Asio, log_lib
  grpc_lib → gRPC, Protobuf, log_lib

视频层 (Video):
  puller_lib → net_lib, log_lib
  camera_lib → net_lib, log_lib
  decoder_lib → FFmpeg, log_lib
  zlmediakit_lib → net_lib, log_lib

处理层 (Processing):
  preprocess_lib → OpenCV, decoder_lib, log_lib
  postprocess_lib → OpenCV, decoder_lib, log_lib
  alg_lib → grpc_lib, OpenCV, log_lib

业务层 (Business):
  videopipeline_lib → puller + decoder + preprocess + postprocess + alg + log
  web_lib → net + zlmediakit + config + log
    ├── api/ (HTTP 路由)
    └── service/ (服务容器)

应用层 (Application):
  MySelfContainedApp → 所有模块
```

---

## 📝 V1.0 详细待办事项

### Phase 1: 架构设计与规划 (Week 1)

#### 1.1 确定 Web Service 的最终结构
- [ ] **设计统一的服务架构**
  - [ ] 定义 `web/service/` 目录结构
  - [ ] 确定服务生命周期管理方式
  - [ ] 设计服务间通信机制
  
- [ ] **制定模块整合策略**
  - [ ] 哪些模块应该成为 Service？
  - [ ] 哪些模块保持为 Library？
  - [ ] 如何避免循环依赖？

**建议结构**：
```
modules/web/
├── include/web/
│   ├── core/              # 核心框架
│   │   ├── application.h         # 应用主类
│   │   ├── service_base.h        # 服务基类
│   │   ├── service_manager.h     # 服务管理器
│   │   └── lifecycle.h           # 生命周期管理
│   ├── services/          # 具体服务实现
│   │   ├── video_pipeline_service.h
│   │   ├── algorithm_service.h
│   │   ├── stream_service.h
│   │   ├── http_server_service.h
│   │   └── database_service.h
│   ├── api/               # HTTP API
│   │   ├── api_router.h
│   │   ├── stream_api.h
│   │   ├── system_api.h
│   │   └── admin_api.h
│   └── config/            # 配置管理
│       └── app_config.h
├── src/
│   ├── core/
│   ├── services/
│   ├── api/
│   └── config/
├── test/
└── CMakeLists.txt
```

#### 1.2 定义服务接口规范
- [ ] **创建 IService 基类**
  ```cpp
  class IService {
  public:
      virtual ~IService() = default;
      virtual std::string name() const = 0;
      virtual bool initialize() = 0;
      virtual bool start() = 0;
      virtual void stop() = 0;
      virtual bool isRunning() const = 0;
  };
  ```

- [ ] **定义服务注册机制**
  - [ ] 自动服务发现
  - [ ] 依赖注入
  - [ ] 启动顺序管理

---

### Phase 2: 核心框架开发 (Week 2-3)

#### 2.1 实现 Application 核心类
- [ ] **创建 `application.h/cpp`**
  - [ ] 单例模式或依赖注入容器
  - [ ] 配置文件加载
  - [ ] 日志系统初始化
  - [ ] 信号处理（graceful shutdown）
  - [ ] 服务生命周期管理

- [ ] **实现服务管理器**
  - [ ] 服务注册表
  - [ ] 依赖解析
  - [ ] 启动/停止顺序控制
  - [ ] 健康检查机制

#### 2.2 重构现有 Service
- [ ] **迁移 `http_server_service`**
  - [ ] 从 `modules/web/src/service/` 移动到新的统一位置
  - [ ] 更新头文件路径
  - [ ] 确保与其他服务解耦

- [ ] **迁移 `zlm_service`**
  - [ ] 封装 ZLMediaKit 管理逻辑
  - [ ] 提供流管理 API
  - [ ] 事件通知机制

- [ ] **迁移 `httpclient_pool_service`**
  - [ ] HTTP 客户端池化管理
  - [ ] 连接复用优化

#### 2.3 创建新的核心服务

##### 2.3.1 VideoPipelineService
- [ ] **封装视频流水线**
  ```cpp
  class VideoPipelineService : public IService {
  private:
      std::unique_ptr<VideoPipelineManager> pipeline_mgr_;
      
  public:
      bool initialize() override;
      bool start() override;
      void stop() override;
      
      // 业务方法
      int createPipeline(const PipelineConfig& config);
      bool removePipeline(int channel_id);
      PipelineStats getStats(int channel_id);
  };
  ```

- [ ] **功能需求**
  - [ ] 多路视频流管理
  - [ ] 动态添加/删除通道
  - [ ] 实时监控统计
  - [ ] 错误恢复机制

##### 2.3.2 AlgorithmService
- [ ] **封装算法处理**
  ```cpp
  class AlgorithmService : public IService {
  private:
      std::unique_ptr<GrpcAlgorithmProcessor> alg_processor_;
      
  public:
      bool initialize() override;
      bool start() override;
      void stop() override;
      
      // 业务方法
      DetectionResult processFrame(const cv::Mat& frame);
      void setAlgorithmConfig(const AlgConfig& config);
  };
  ```

- [ ] **功能需求**
  - [ ] gRPC 连接管理
  - [ ] 异步结果回调
  - [ ] 失败重试机制
  - [ ] 性能监控

##### 2.3.3 StreamService
- [ ] **统一管理视频流**
  - [ ] 拉流器管理（ZLMPuller）
  - [ ] 推流管理（如果需要）
  - [ ] 流状态监控
  - [ ] 断线重连

##### 2.3.4 DatabaseService
- [ ] **封装 SQLite 操作**
  - [ ] 连接池管理
  - [ ] ORM 简化层
  - [ ] 数据迁移支持
  - [ ] 备份/恢复

---

### Phase 3: API 层完善 (Week 4)

#### 3.1 RESTful API 设计
- [ ] **定义 API 规范**
  ```
  GET    /api/v1/streams              # 获取所有流列表
  POST   /api/v1/streams              # 创建新流
  GET    /api/v1/streams/{id}         # 获取流详情
  DELETE /api/v1/streams/{id}         # 删除流
  PUT    /api/v1/streams/{id}/config  # 更新流配置
  
  GET    /api/v1/pipelines            # 获取所有流水线
  POST   /api/v1/pipelines            # 创建流水线
  GET    /api/v1/pipelines/{id}/stats # 获取流水线统计
  
  GET    /api/v1/system/health        # 健康检查
  GET    /api/v1/system/metrics       # 系统指标
  POST   /api/v1/system/reload-config # 重载配置
  ```

- [ ] **实现 API Handler**
  - [ ] StreamApiHandler
  - [ ] PipelineApiHandler
  - [ ] SystemApiHandler
  - [ ] AdminApiHandler（可选）

#### 3.2 WebSocket 实时通信
- [ ] **实现 WebSocket 服务**
  - [ ] 实时视频帧推送
  - [ ] 检测结果推送
  - [ ] 系统事件通知

- [ ] **消息协议设计**
  ```json
  {
    "type": "detection_result",
    "timestamp": 1234567890,
    "channel_id": 1,
    "frame_id": "ch1_frame100",
    "boxes": [...]
  }
  ```

#### 3.3 API 文档
- [ ] **生成 OpenAPI/Swagger 文档**
  - [ ] 使用 swagger-cpp 或手动编写
  - [ ] 包含所有端点说明
  - [ ] 请求/响应示例
  - [ ] 错误码说明

---

### Phase 4: 配置与管理 (Week 5)

#### 4.1 统一配置管理
- [ ] **创建 AppConfig**
  ```yaml
  # config/app.yaml
  server:
    host: 0.0.0.0
    port: 8080
    
  services:
    video_pipeline:
      enabled: true
      max_channels: 16
      decoder_threads: 2
      
    algorithm:
      enabled: true
      grpc_server: localhost:50053
      target_fps: 10
      
    database:
      enabled: true
      path: data/app.db
      
  logging:
    level: info
    dir: logs
    max_files: 10
  ```

- [ ] **配置验证**
  - [ ] 必填项检查
  - [ ] 类型验证
  - [ ] 范围验证

#### 4.2 运行时管理
- [ ] **实现配置热重载**
  - [ ] 监听配置文件变化
  - [ ] 安全地重新加载配置
  - [ ] 回滚机制

- [ ] **实现管理 API**
  - [ ] 动态调整参数
  - [ ] 查看运行时状态
  - [ ] 导出诊断信息

---

### Phase 5: 监控与日志 (Week 6)

#### 5.1 增强日志系统
- [ ] **结构化日志**
  - [ ] JSON 格式输出（可选）
  - [ ] 日志级别动态调整
  - [ ] 日志轮转优化

- [ ] **审计日志**
  - [ ] 记录关键操作
  - [ ] 用户行为追踪
  - [ ] 安全事件记录

#### 5.2 性能监控
- [ ] **实现 Metrics 收集**
  ```cpp
  class MetricsCollector {
  public:
      void recordFrameDecoded(int channel_id, double latency_ms);
      void recordGrpcRequest(double latency_ms, bool success);
      void recordHttpEndpoint(const std::string& endpoint, int status_code);
      
      MetricsSnapshot getSnapshot();
  };
  ```

- [ ] **暴露监控端点**
  - [ ] `/metrics` - Prometheus 格式
  - [ ] `/stats` - 人类可读格式
  - [ ] `/health` - 健康检查

#### 5.3 告警机制
- [ ] **定义告警规则**
  - [ ] 服务崩溃
  - [ ] 高错误率
  - [ ] 性能下降
  - [ ] 资源不足

- [ ] **告警通知**
  - [ ] 邮件通知
  - [ ] Webhook
  - [ ] 日志标记

---

### Phase 6: 测试与质量保证 (Week 7-8)

#### 6.1 单元测试
- [ ] **为核心服务编写测试**
  - [ ] VideoPipelineService 测试
  - [ ] AlgorithmService 测试
  - [ ] StreamService 测试
  - [ ] 覆盖率目标：>80%

#### 6.2 集成测试
- [ ] **端到端测试场景**
  - [ ] 完整视频流处理流程
  - [ ] gRPC 通信测试
  - [ ] HTTP API 测试
  - [ ] WebSocket 实时通信测试

#### 6.3 压力测试
- [ ] **性能基准测试**
  - [ ] 最大并发流数
  - [ ] FPS 稳定性
  - [ ] 内存使用监控
  - [ ] CPU 使用率

- [ ] **长时间运行测试**
  - [ ] 24小时稳定性测试
  - [ ] 内存泄漏检测
  - [ ] 资源泄漏检测

#### 6.4 文档测试
- [ ] **验证所有示例代码**
  - [ ] API 示例可运行
  - [ ] 配置示例有效
  - [ ] 部署步骤准确

---

### Phase 7: 部署与运维 (Week 9)

#### 7.1 构建优化
- [ ] **Release 构建配置**
  - [ ] 优化编译选项
  - [ ] 符号剥离
  - [ ] 二进制体积优化

- [ ] **依赖打包**
  - [ ] 静态链接关键库
  - [ ] DLL 依赖收集脚本
  - [ ] vcpkg 依赖导出

#### 7.2 部署脚本
- [ ] **Windows 部署**
  ```powershell
  # deploy/windows/install.ps1
  - 安装运行时依赖
  - 配置服务
  - 设置开机自启
  ```

- [ ] **Linux 部署**（如果支持）
  ```bash
  # deploy/linux/install.sh
  - systemd 服务文件
  - 权限配置
  - 日志轮转配置
  ```

- [ ] **Docker 支持**（可选）
  ```dockerfile
  FROM mcr.microsoft.com/windows/servercore:ltsc2022
  COPY bin/ /app/
  COPY config/ /app/config/
  ENTRYPOINT ["MySelfContainedApp.exe"]
  ```

#### 7.3 运维文档
- [ ] **编写运维手册**
  - [ ] 安装指南
  - [ ] 配置说明
  - [ ] 故障排查
  - [ ] 常见问题

- [ ] **编写用户手册**
  - [ ] API 使用指南
  - [ ] 最佳实践
  - [ ] 示例项目

---

### Phase 8: 代码清理与优化 (Week 10)

#### 8.1 代码质量
- [ ] **代码审查**
  - [ ] 移除调试代码
  - [ ] 统一代码风格
  - [ ] 添加缺失的注释

- [ ] **性能优化**
  - [ ] 热点函数优化
  - [ ] 内存分配优化
  - [ ] 减少拷贝（零拷贝优化）

#### 8.2 文档完善
- [ ] **README.md**
  - [ ] 项目介绍
  - [ ] 快速开始
  - [ ] 架构图
  - [ ] 贡献指南

- [ ] **CHANGELOG.md**
  - [ ] V1.0 变更列表
  - [ ] 已知问题
  - [ ] 升级指南

- [ ] **CONTRIBUTING.md**
  - [ ] 开发环境搭建
  - [ ] 代码规范
  - [ ] PR 流程

#### 8.3 版本标记
- [ ] **Git Tag**
  ```bash
  git tag -a v1.0.0 -m "Release V1.0.0 - Production Ready"
  git push origin v1.0.0
  ```

---

## 📊 里程碑时间表

| Phase | 任务 | 预计时间 | 完成标准 |
|-------|------|----------|----------|
| **Phase 1** | 架构设计 | Week 1 | 设计文档评审通过 |
| **Phase 2** | 核心框架 | Week 2-3 | 所有服务可正常运行 |
| **Phase 3** | API 完善 | Week 4 | API 文档完整，测试通过 |
| **Phase 4** | 配置管理 | Week 5 | 配置热重载工作正常 |
| **Phase 5** | 监控日志 | Week 6 | 监控面板可用 |
| **Phase 6** | 测试 QA | Week 7-8 | 测试覆盖率 >80% |
| **Phase 7** | 部署运维 | Week 9 | 一键部署脚本可用 |
| **Phase 8** | 清理优化 | Week 10 | 代码审查通过，打 Tag |

**总计**: 10 周（约 2.5 个月）

---

## ✅ V1.0 验收标准

### 功能完整性
- [ ] 所有核心服务正常运行
- [ ] HTTP API 完全可用
- [ ] WebSocket 实时通信稳定
- [ ] gRPC 算法调用正常
- [ ] 视频流处理流畅（无卡顿）

### 性能指标
- [ ] 支持至少 8 路 1080p@30fps 视频流
- [ ] API 响应时间 < 100ms（P95）
- [ ] gRPC 延迟 < 50ms（P95）
- [ ] 内存占用 < 2GB（8路流）
- [ ] CPU 使用率 < 80%（8路流）

### 稳定性
- [ ] 7x24 小时无故障运行
- [ ] 自动重连机制可靠
- [ ]  graceful shutdown 无资源泄漏
- [ ] 异常情况下不崩溃

### 文档完整性
- [ ] README.md 完整
- [ ] API 文档完整
- [ ] 部署指南清晰
- [ ] 故障排查手册可用

### 代码质量
- [ ] 单元测试覆盖率 > 80%
- [ ] 无编译器警告
- [ ] 通过静态代码分析
- [ ] 代码审查通过

---

## 🚀 快速开始清单

完成以下任务即可宣布 V1.0 Beta：

- [ ] 整合所有服务到 `modules/web/service/`
- [ ] 实现 Application 核心类
- [ ] 完成主要 API 端点
- [ ] 编写基础测试
- [ ] 创建部署脚本
- [ ] 编写用户文档

---

## 📞 支持与反馈

- **问题反馈**: GitHub Issues
- **技术讨论**: 内部技术群
- **文档改进**: PR Welcome

---

**最后更新**: 2026-04-12  
**版本**: V1.0 Draft  
**状态**: Planning  

---

## 🎯 下一步行动

1. **立即执行**：
   - [ ] 评审此文档
   - [ ] 确认架构设计方案
   - [ ] 分配任务给团队成员

2. **本周内**：
   - [ ] 创建 `modules/web/include/web/core/` 目录
   - [ ] 实现 `IService` 基类
   - [ ] 实现 `ServiceManager`

3. **下周开始**：
   - [ ] 迁移第一个服务（建议从 `VideoPipelineService` 开始）
   - [ ] 更新 CMakeLists.txt
   - [ ] 验证编译和运行

---

**加油！V1.0 就在眼前！** 🎉
